apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rockbot.agentServiceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "rockbot.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent
# The agent has no Kubernetes API permissions — script execution is delegated
# to the Script Manager pod via the RabbitMQ message bus.
automountServiceAccountToken: false
