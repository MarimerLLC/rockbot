apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rockbot.agentServiceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "rockbot.labels" . | nindent 4 }}
    app.kubernetes.io/component: agent
# automountServiceAccountToken defaults to true — the agent needs the token
# to authenticate against the Kubernetes API when creating script pods.
